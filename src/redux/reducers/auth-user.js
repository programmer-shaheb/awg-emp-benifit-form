const initialState = {
  error: null,
  isError: false,
  isLoading: false,
  user: null
};

const reducer = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};



export default reducer;

export const logout = () => {};
